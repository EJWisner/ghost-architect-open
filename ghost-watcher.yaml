ghost_watcher:
  version: "1.0"
  enabled: true
  trigger:
    branches:
      - feature/**
      - v7-unified
  scans:
    blast_radius: true
    conflict_detection: true
    ghost_brief: true
    conflict_verify: false
    # Generate LLM-authored remediation prompts per finding (default: true)
    detailed_prompts: true
  batch:
    poll_interval_seconds: 30
    timeout_minutes: 90
  portal:
    repo: https://github.com/EJWisner/ghost-reports-portal-test
    token_env: GHOST_PORTAL_TOKEN
  notifications:
    pr_comment: true
    email:
      - ejwisner@gmail.com
  exclude:
    - "test/**"
    - "tests/**"
    - "test-*.js"
    - "prompts/**"
    - "prompts-extracted/**"
    - "scripts/**"
    - "audit-reports/**"
    - "*.png"
    - "*.jpg"
    - "*.pdf"
  team:
    config: team.yaml
