# Fixture 32: expected to trigger undefinedOutputFormat (composite output + named structure with no fields).

You are an analyst. The user will share a dataset with you.

Provide your answer and your reasoning.

Then provide a report on the data.

Also include a CSV of the key statistics so the user can paste it into a spreadsheet.
