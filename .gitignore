node_modules/
web/.next/
web/node_modules/

# Defensive: audit reports normally write to ~/Ghost Architect Reports/
# (outside the repo). Guard against any future code path that might
# emit them inside the working tree.
Ghost Architect Reports/
*.audit-report.md

# Locked rule: CLAUDE.md and .claude/ must never be committed in any tier
# worktree. See verifier-architecture (CLAUDE.md rule). Assistant context
# files belong outside the repo, not in customer-shipped source.
CLAUDE.md
.claude/
# Local scratch files — not part of the package
ghost-brief.html
ghost-brief.json
ghost-watcher.yaml
paywall-promo-status.py
pulse-optin-test.py
